enum = redirect	redirect viene skippato, inserisci solo stringa successiva.


	Redirect_input = >
	Redirect_input_heredoc = <<
	Redirect_output = <
	Redirect_output_append = >>

enum = pipe NULL se trova |

enum = Program e tutto il resto con tutte le stringhe a seguire fino a un pipe