solo spazi vuoti da exit status 1 quando dovrebbe essere 0

il lexer legge uno, e solo uno, spazio alla fine quando non dovrebbe

shell dentro shell si bugga il del

cat > testfile | grep a si bugga

$? + $? da problemi

"export PATH=""
env | grep PATH"  ha un leak
i comandi senza PATH danno leak

grep newfile a da leak


EOF si rompe

 
