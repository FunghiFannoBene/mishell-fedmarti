
shell dentro shell si bugga il del

"export PATH=""
env | grep PATH"  ha un leak
i comandi senza PATH danno leak


shell dentro shell si bugga il del

"export PATH=""
env | grep PATH"  ha un leak
i comandi senza PATH danno leak

grep newfile a da leak


EOF si rompe
grep newfile a da leak


EOF si rompe
